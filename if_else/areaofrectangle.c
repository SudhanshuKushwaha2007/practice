#include <stdio.h>
int main() {
    int x,y,z;
    printf("Enter the value of width : ");
    printf("Enter the value of length : ");
    scanf("%d", &y);
    z=x*y;
    printf("Area of rectangle is %d",z);
    return (0);
}